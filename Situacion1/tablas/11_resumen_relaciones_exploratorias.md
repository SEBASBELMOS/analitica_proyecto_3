# Resumen de relaciones exploratorias multifuente

Se analizaron correlaciones de Pearson entre contaminantes medidos en estaciones terrestres y covariables meteorológicas, satelitales y ópticas disponibles en el panel final model-safe.

Estas relaciones tienen carácter exploratorio y no deben interpretarse como causalidad. Su función principal es verificar que el panel integrado permite estudiar asociaciones ambientales relevantes.

## no2_insitu

- `surface_solar_radiation_downwards_j_m2` presenta una correlación positiva de 0.3831 con `no2_insitu` usando 26259 pares válidos.
- `surface_pressure_hpa` presenta una correlación negativa de -0.3438 con `no2_insitu` usando 26259 pares válidos.
- `wind_speed_10m` presenta una correlación positiva de 0.1609 con `no2_insitu` usando 26259 pares válidos.
- `wind_direction_10m_deg` presenta una correlación negativa de -0.1519 con `no2_insitu` usando 26259 pares válidos.
- `temperature_2m_c` presenta una correlación positiva de 0.1439 con `no2_insitu` usando 26259 pares válidos.

## so2_insitu

- `surface_pressure_hpa` presenta una correlación negativa de -0.1025 con `so2_insitu` usando 132415 pares válidos.
- `dewpoint_2m_c` presenta una correlación negativa de -0.0984 con `so2_insitu` usando 132415 pares válidos.
- `temperature_2m_c` presenta una correlación negativa de -0.0632 con `so2_insitu` usando 132415 pares válidos.
- `wind_direction_10m_deg` presenta una correlación positiva de 0.0571 con `so2_insitu` usando 132415 pares válidos.
- `surface_solar_radiation_downwards_j_m2` presenta una correlación negativa de -0.0490 con `so2_insitu` usando 132415 pares válidos.

## o3_insitu

- `relative_humidity_pct` presenta una correlación negativa de -0.2044 con `o3_insitu` usando 191257 pares válidos.
- `temperature_2m_c` presenta una correlación positiva de 0.1293 con `o3_insitu` usando 191257 pares válidos.
- `surface_solar_radiation_downwards_j_m2` presenta una correlación negativa de -0.1197 con `o3_insitu` usando 191257 pares válidos.
- `surface_pressure_hpa` presenta una correlación positiva de 0.1039 con `o3_insitu` usando 191257 pares válidos.
- `s2_ndbi` presenta una correlación negativa de -0.0829 con `o3_insitu` usando 185541 pares válidos.
